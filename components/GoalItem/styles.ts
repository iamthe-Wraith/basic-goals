import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  listItem: {
    marginBottom: 5,
    borderRadius: 6,
    backgroundColor: '#5edaec',
  },
  listItemText: {
    padding: 10,
  },
  pressedItem: {
    opacity: 0.5,
  }
});